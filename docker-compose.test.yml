services:
  app:
    ports:
      - "127.0.0.1:8443:8443"

    tmpfs:
      - /var/lib/tuchka-server

    volumes:
      - ./certs:/certs:ro
  migrations:
    volumes:
      - ./migrations:/migrations